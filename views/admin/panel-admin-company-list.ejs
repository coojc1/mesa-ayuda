<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/bootstrap-5.3.3-dist/css/bootstrap.min.css">
    <script src="https://kit.fontawesome.com/c55ac3095c.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/admin/nav-panel.css">
    <link rel="stylesheet" href="/css/admin/panel-admin-company-list.css">
    <link rel="shortcut icon" href="/res/logo-5.png" type="image/x-icon">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <title>Empresas</title>
</head>
<body>
    <div class="container-fluid">
        <%- include("nav-panel.ejs") %>

        <div class="content">
            <div class="div-table">
                <% if(empresas.length === 0) { %>
                    <p>No hay empresas registradas para mostrar. 😥😥</p>
                <% } else { %>
                    <table class="table table-striped table-hover">
                        <tr>
                            <th>#</th>
                            <th>Razón</th>
                            <th>Correo representante</th>
                            <th>Correo empresa</th>
                            <th>Teléfono</th>
                            <th>Póliza</th>
                            <th>No. Candado</th>
                            <th>Acción</th>
                        </tr>
                        <% for(let i = 0; i < empresas.length; i++){ %>
                            <tr>
                                <td><%= empresas[i].id_usuario_pk %></td>
                                <td><%= empresas[i].nombre %></td>
                                <td>
                                    <a name="" id="" class="btn btn-primary" href="mailto:<%= empresas[i].correo %>" role="button">
                                        <span class="badge badge-primary"><%= empresas[i].correo %></span>
                                    </a>
                                </td>
                                <td>
                                    <a name="" id="" class="btn btn-primary" href="mailto:<%= empresas[i].correo_empresarial %>" role="button">
                                        <span class="badge badge-primary"><%= empresas[i].correo_empresarial %></span>
                                    </a>
                                <td><%= empresas[i].telefono %></td>
                                <td>
                                    <% if(empresas[i].poliza === "Si") { %>
                                        <span class="badge rounded-pill bg-success">
                                            <i class="fas fa-shield-alt    "></i> <%= empresas[i].poliza %>
                                        </span>
                                    <% } else { %>
                                        <span class="badge rounded-pill bg-danger">
                                            <i class="fas fa-shield-alt    "></i> <%= empresas[i].poliza %>
                                        </span>
                                    <% } %>
                                </td>
                                <td><%= empresas[i].no_candado %></td>
                                <td>
                                    <div class="buttons">
                                        <a name="" id="" class="btn btn-link" href="/dashboard-admin/company-list/company-info/<%= empresas[i].id_usuario_pk %>" role="button">
                                            <i class="fa fa-eye" aria-hidden="true"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        <% } %>
                    </table>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>