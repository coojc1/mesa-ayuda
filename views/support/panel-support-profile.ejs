<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/res/logo-5.png" type="image/x-icon">
    <link rel="stylesheet" href="/bootstrap-5.3.3-dist/css/bootstrap.min.css">
    <script src="https://kit.fontawesome.com/c55ac3095c.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/support/panel-support-profile.css">
    <link rel="stylesheet" href="/css/support/nav-support.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <title>
        <%= name %>
    </title>
</head>

<body>
    <div class="container-fluid">
        <%- include("nav-support.ejs") %>

            <div class="content">
                <form action="/dashboard-support/profile-update" method="post">
                    <div class="icon-holder">
                        <div class="icon">
                            <i class="fas fa-headset"></i>
                            <h3>
                                <%= support[0].nombre %>
                            </h3>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for=""><i class="fa fa-user-circle-o" aria-hidden="true"></i> Nombre:</label>
                        <input type="text" name="nombre" id="" class="form-control" value="<%= support[0].nombre %>" required>

                        <label for=""><i class="fas fa-mail-bulk"></i> Correo:</label>
                        <input type="email" name="correo" id="" class="form-control" value="<%= support[0].correo %>"
                            required>

                        <label for=""><i class="fa fa-key" aria-hidden="true"></i> Contraseña:</label>
                        <input type="text" name="password" id="" class="form-control" value="<%= support[0].password %>"
                            required>

                        <p>
                            <i class="fas fa-chart-bar"></i> Categoría:
                            <% if(support[0].categoria==="Básico" ) { %>
                                <span class="badge bg-success rounded-pill">
                                    <i class="fa-solid fa-chart-simple"></i>
                                    <%= support[0].categoria %>
                                </span>
                                <% } else if(support[0].categoria==="Intermedio" ) { %>
                                    <span class="badge bg-warning rounded-pill">
                                        <i class="fa-solid fa-chart-simple"></i>
                                        <%= support[0].categoria %>
                                    </span>
                                    <% } else { %>
                                        <span class="badge bg-danger rounded-pill">
                                            <i class="fa-solid fa-chart-simple"></i>
                                            <%= support[0].categoria %>
                                        </span>
                                        <% } %>
                        </p>

                        <button type="submit" name="" id="" class="btn btn-primary" btn-lg btn-block">
                            <i class="fa fa-paper-plane-o" aria-hidden="true"></i> Guardar Cambios
                        </button>
                    </div>
                </form>
            </div>
    </div>
</body>

</html>